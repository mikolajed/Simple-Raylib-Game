// takes no arguments, as a result returns integer number of players
int get_nbr_of_players();

// takes integer pointer to array's first index and number n of players to be initialized 
void init_players(struct player* array, int n);

// takes no aruments, returns the size of the map's x dimension
int get_x_dimension();

// takes no aruments, returns the size of the map's y dimension
int get_y_dimension();

//
